      <section className="customer-welcome"><div><span className="eyebrow">مرحبًا، {customerName}</span><h1>احتياج متجرك<br/><em>جاهز للطلب.</em></h1><p>أسعار الجملة والمخزون والخصومات المصرح بها لحسابك في مكان واحد.</p><div className="welcome-actions">{uiConfig.showQuickOrder && <button onClick={() => setQuickOrderOpen(true)}>⚡ طلب سريع</button>}{uiConfig.showTemplates && <button className="secondary" onClick={() => setSection('templates')}>↻ إعادة طلب محفوظ</button>}</div></div>{uiConfig.showCredit && <div className="credit-mini"><span>المتاح الائتماني</span><strong>{finance ? formatMoney(finance.available) : '—'}</strong><small>{finance ? `من حد ${formatMoney(finance.creditLimit)} ${currencyLabel(finance.currency)}` : 'المعلومات المالية ستظهر بعد مزامنة المركز المالي'}</small></div>}</section>
      {runtimeError && <div className="error-banner">{runtimeError}</div>}
      <section className="customer-nav"><button className={section === 'catalog' ? 'nav-pill active' : 'nav-pill'} onClick={() => setSection('catalog')}>الكتالوج</button><button className={section === 'orders' ? 'nav-pill active' : 'nav-pill'} onClick={() => setSection('orders')}>طلباتي <span>{orders.length}</span></button>{uiConfig.showTemplates && <button className={section === 'templates' ? 'nav-pill active' : 'nav-pill'} onClick={() => setSection('templates')}>المحفوظة</button>}<button className={section === 'account' ? 'nav-pill active' : 'nav-pill'} onClick={() => setSection('account')}>حسابي</button>{uiConfig.showCredit && <button className={section === 'finance' ? 'nav-pill active' : 'nav-pill'} onClick={() => setSection('finance')}>المركز المالي</button>}</section>
      {section === 'catalog' && <><section className="catalog-toolbar"><div><h2>الكتالوج السريع</h2><p>ابحث بالاسم أو SKU أو الباركود، ثم أضف الكمية مباشرة.</p></div><div className="toolbar-actions">{uiConfig.showExcel && <label className="upload-button">رفع Excel<input type="file" accept=".xlsx,.xls" onChange={importExcel} hidden/></label>}{uiConfig.showQuickOrder && <button onClick={() => setQuickOrderOpen(true)}>طلب سريع</button>}</div></section>{uiConfig.showCategories && <div className="category-strip">{categories.map((item) => <button key={item.id ?? 'all'} className={item.id === categoryId ? 'category-chip active' : 'category-chip'} onClick={() => setCategoryId(item.id)}>{item.name}</button>)}</div>}<section className="product-grid" aria-busy={catalogLoading}>{catalogLoading && <div className="empty-state">جارٍ تحميل الكتالوج…</div>}{!catalogLoading && products.map((product) => { const price = effectivePrice(product); const tier = nextTier(product, 1); return <article className="b2b-product-card" key={product.id}><div className="product-visual">{product.imageUrl ? <img src={product.imageUrl} alt={product.name} loading="lazy"/> : <span>{product.name.slice(0,1)}</span>}{uiConfig.showInventory && <span className={product.availableQuantity > 20 ? 'stock-badge good' : product.availableQuantity > 0 ? 'stock-badge low' : 'stock-badge out'}>{product.availableQuantity > 20 ? 'متوفر' : product.availableQuantity > 0 ? 'مخزون منخفض' : 'غير متوفر'}</span>}</div><div className="b2b-product-info"><div className="sku-row"><span>{product.category}</span><code>{product.sku}</code></div><h3>{product.name}</h3><p>{product.description ?? 'توريد جملة مباشر من مخزون الأغبري.'}</p><div className="unit-row">{UNIT_OPTIONS.map((unit) => <span key={unit} className={product.unit === unit ? 'unit-tag active' : 'unit-tag'}>{unit}</span>)}</div><div className="price-row"><div><small>سعر حسابك</small><strong>{price > 0 ? `${formatMoney(price)} ${currencyLabel()}` : 'غير متاح'}</strong></div><div className="product-card-actions"><button className="secondary-action" onClick={() => { setSelectedProductId(product.id); setDetailQuantity(1); }}>التفاصيل</button><button disabled={price <= 0 || product.availableQuantity < 1 || !isOnline} onClick={() => void addToCart(product)}>+ إضافة</button></div></div>{uiConfig.showTieredPricing && (priceTiers[product.id]?.length ?? 0) > 0 && <div className="tier-list" aria-label="شرائح الأسعار">{(priceTiers[product.id] ?? []).slice(0, 4).map((item) => <span key={item.min_quantity}>{item.min_quantity}+ · {formatMoney(item.unit_price)} {currencyLabel(item.currency)}</span>)}</div>}{uiConfig.showSavingsCalculator && tier && <div className="tier-hint">✦ {savingHint(product)}</div>}</div></article>; })}{!catalogLoading && !products.length && <div className="empty-state">لا توجد أصناف مطابقة للبحث الحالي.</div>}</section></>}
      {section === 'orders' && <section className="content-card"><div className="section-title"><div><span className="eyebrow">المتابعة</span><h2>طلباتك وشحناتك</h2></div><span>{orders.length} طلبًا</span></div>{ordersLoading ? <div className="empty-state">جارٍ تحميل الطلبات…</div> : ordersError ? <div className="error-banner">{ordersError}</div> : !orders.length ? <div className="empty-state">لا توجد طلبات سابقة بعد.</div> : <div className="orders-list">{orders.map((order) => { const idx = statusIndex(order.status); return <article className="order-card" key={order.id}><div className="order-head"><strong>طلب #{order.order_number}</strong><span>{formatMoney(order.total)} {currencyLabel(order.currency)}</span></div><small>{new Date(order.created_at).toLocaleString('ar-YE')}</small><div className="timeline">{STATUS_STEPS.map((step, i) => <div key={step} className={idx >= i ? 'timeline-step done' : 'timeline-step'}><span>{idx >= i ? '✓' : '•'}</span><label>{step === 'pending' ? 'جديد' : step === 'confirmed' ? 'مؤكد' : step === 'preparing' ? 'قيد التجهيز' : step === 'ready' ? 'قيد الشحن' : 'مكتمل'}</label></div>)}</div><div className="order-footer"><span className={`status status-${order.status}`}>{STATUS_LABELS[order.status]}</span><div className="order-actions"><button className="secondary-action" onClick={() => void openOrderDetails(order)}>التفاصيل</button><button onClick={() => void reorder(order)}>إعادة الطلب</button></div></div></article>; })}</div>}</section>}
      {section === 'templates' && uiConfig.showTemplates && <section className="content-card"><div className="section-title"><div><span className="eyebrow">طلباتك المتكررة</span><h2>الطلبات المحفوظة</h2></div><button onClick={() => setSection('catalog')}>+ أنشئ من السلة</button></div>{templates.length ? <div className="template-grid">{templates.map((template) => <article className="template-card" key={template.id}><span className="template-icon">▦</span><h3>{template.name}</h3><small>{template.lines.length} أصناف · {template.branchLabel}</small><div><button onClick={() => void applyTemplate(template)}>إعادة الطلب</button><button className="ghost" onClick={() => { void (async () => { try { await deleteOrderTemplate(template.id); setTemplates((current) => current.filter((item) => item.id !== template.id)); setRuntimeError(null); } catch (error) { setRuntimeError(error instanceof Error ? error.message : 'تعذر حذف الطلب المحفوظ.'); } })(); }}>حذف</button></div></article>)}</div> : <div className="empty-state">لا توجد طلبات محفوظة. أضف أصنافًا إلى السلة ثم احفظها كقائمة إعادة طلب.</div>}<div className="template-save"><input value={templateName} onChange={(e) => setTemplateName(e.target.value)} placeholder="اسم قائمة إعادة الطلب"/><button disabled={!templateName.trim() || !cart.length} onClick={() => void saveTemplate()}>حفظ السلة كقائمة</button></div></section>}
      {section === 'account' && <section className="content-card"><div className="section-title"><div><span className="eyebrow">الحساب</span><h2>بيانات حسابك</h2></div><span>معلومات تشغيلية للقراءة فقط</span></div><div className="finance-grid"><div><small>العميل</small><strong>{customerName}</strong></div><div><small>الفئة</small><strong>{customerTier === 'wholesale' ? 'جملة' : customerTier}</strong></div><div><small>الحالة</small><strong>{isOnline ? 'متصل' : 'دون اتصال'}</strong></div></div><div className="account-note"><strong>تجربة آمنة أولًا</strong><p>الأسعار والصلاحيات والطلبات النهائية يقررها الخادم، ويمكنك العودة للكتالوج في أي وقت دون فقدان السلة.</p></div></section>}
      {section === 'finance' && uiConfig.showCredit && <section className="content-card"><div className="section-title"><div><span className="eyebrow">الثقة المالية</span><h2>المركز المالي</h2></div><button onClick={downloadStatement} disabled={!finance}>تنزيل الكشف</button></div>{financeLoading ? <div className="empty-state">جارٍ تحميل المركز المالي…</div> : <>{finance ? <><div className="finance-grid"><div><small>الحد الائتماني</small><strong>{formatMoney(finance.creditLimit)} {currencyLabel(finance.currency)}</strong></div><div><small>الرصيد المستحق</small><strong>{formatMoney(finance.outstanding)} {currencyLabel(finance.currency)}</strong></div><div><small>المتاح للشراء</small><strong>{formatMoney(finance.available)} {currencyLabel(finance.currency)}</strong></div></div><div className="ledger-table">{finance.entries.map((entry) => <div className="ledger-row" key={entry.id}><span>{entry.reference ?? '—'}</span><strong>{entry.description}</strong><span>{entry.debit ? `مدين ${formatMoney(entry.debit)}` : `دائن ${formatMoney(entry.credit)}`}</span><span>{entry.due_date ?? '—'}</span><span className={`status status-${entry.status}`}>{entry.status === 'overdue' ? 'متأخر' : entry.status === 'paid' ? 'مسدد' : 'مفتوح'}</span></div>)}</div></> : <div className="empty-state">لا توجد بيانات مركز مالي متاحة لحسابك حاليًا.</div>}</>}</section>}
    </main>
    {quickOrderOpen && uiConfig.showQuickOrder && <div className="modal-backdrop" onClick={() => setQuickOrderOpen(false)}><section className="modal" onClick={(e) => e.stopPropagation()}><div className="modal-head"><div><span className="eyebrow">Quick Order</span><h2>الطلب السريع</h2></div><button onClick={() => setQuickOrderOpen(false)}>×</button></div><p>أدخل SKU والكمية لإضافة أصناف كثيرة بسرعة.</p><QuickOrderEditor products={products} onAdd={addToCart} onClose={() => setQuickOrderOpen(false)}/></section></div>}
    {cartOpen && <div className="drawer-backdrop" onClick={() => setCartOpen(false)}><aside className="cart-drawer" onClick={(e) => e.stopPropagation()}><div className="drawer-head"><div><span className="eyebrow">سلة الشراء</span><h2>{cartCount} وحدة</h2></div><button onClick={() => setCartOpen(false)}>×</button></div>{!cart.length ? <div className="empty-state">السلة فارغة.</div> : <>{cart.map((line) => <div className="drawer-line" key={line.product.id}><div><strong>{line.product.name}</strong><small>{formatMoney(line.unitPrice)} / {line.product.unit}</small></div><div className="quantity"><button onClick={() => void updateQuantity(line.product.id, line.quantity - 1)}>−</button><span>{line.quantity}</span><button onClick={() => void updateQuantity(line.product.id, line.quantity + 1)}>+</button></div><strong>{formatMoney(line.unitPrice * line.quantity)}</strong></div>)}<div className="drawer-total"><span>الإجمالي</span><strong>{formatMoney(total)} ر.ي</strong></div>
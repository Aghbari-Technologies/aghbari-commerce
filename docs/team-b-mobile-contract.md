# Team B Mobile Contract

The customer shell must remain usable at 375px viewport width. The browser contract checks that document scroll width does not exceed viewport width and that login controls remain visible.
